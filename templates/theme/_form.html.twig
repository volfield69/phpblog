<div class="row">
    <div class="col-sm-12 ">
        {{ form_start(form) }}
        <div class="card">
            <div class="card-body">
                <div class="form-group">
                    {{ form_label(form.title) }}
                    {{ form_widget(form.title) }}
                    {{ form_errors(form.title) }}
                </div>
            </div>
        </div>
        <button type="submit" class="btn btn-primary btn-suivant float-right">{{ 'base.save'|trans }}</button>
        {{ form_end(form) }}
    </div>
</div>
